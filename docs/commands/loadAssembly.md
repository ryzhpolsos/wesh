# `loadAssembly`

## Синтаксис
```
loadAssembly <сборка>
```

- **сборка**: название загружаемой сборки.

Загружает указанную .NET-сборку по имени.

## Примеры
```
loadAssembly System.Windows.Forms
```
Загрузка сборки `System.Windows.Forms`, необходимой для работы с WinForms.
